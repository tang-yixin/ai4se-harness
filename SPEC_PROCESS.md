# SPEC_PROCESS.md — 规约与计划过程文档

> 记录与 Superpowers 协作生成 SPEC 和 PLAN 的全过程，包括 brainstorming 关键节点、智能体的优劣表现、人工决策与修正。

---

## 一、Brainstorming 流程概览

本次 brainstorming 遵循 Superpowers 的标准流程：澄清问题（每次一个）→ 提出方案 → 逐节呈现设计 → 逐节签字确认 → 写入 SPEC → 自审 → 用户审阅。

整个过程跨越 10+ 轮交互，从"我想要构建一个 Coding Agent Harness"这一模糊意图出发，逐步收敛到一份包含 11 个章节、所有机制均可 mock 测试的精确 SPEC。

---

## 二、关键决策节点

### 节点 1：编程语言选型 — TypeScript vs Python

**智能体的初始推荐：** TypeScript，理由是全栈统一（CLI + WebUI 同语言）+ 成熟 LLM SDK + 静态类型对 TDD 友好。

**我的追问：** "大部分代码是交给 AI 写，这两个语言对 AI 来说哪个更简单、出错率更低？"

**智能体的深入分析：**
- Python 初始生成快但隐性错误多（动态类型 → 不符合"机制必须是确定性代码"的要求）
- TypeScript 编译期兜底，对 TDD + subagent 迭代更友好——每次自我修正循环更快更准
- 核心判据：harness 不是"让 AI 写一次就完"，而是大量 subagent 迭代，TS 的初始摩擦是"一次性成本"

**我的决策：** 采纳 TypeScript。

**反思：** 这个问题的质量取决于追问。如果我直接接受初始推荐，不会意识到"AI 生成代码的出错模式"这个维度——而这个维度恰好是 harness 项目的核心命题。

---

### 节点 2：深度维度选择 — 治理护栏

**智能体的推荐：** 三个方向（治理护栏 / 反馈闭环 / 工具系统），推荐治理——因为护栏的 if-else 决策树 + HITL 状态机天然适合确定性和 mock 测试。

**我的决策：** 立即采纳。

**反思：** 这个决策做得很快，因为智能体的分析与我自己的预判一致。但当时我并没有充分理解"三层纵深防御"意味着什么——这个细节是在后续设计展开时才逐步清晰的。

---

### 节点 3：界面形态 — CLI 优先 vs WebUI 为主

**智能体给了三个选项：** CLI 优先 / WebUI 为主 / 纯 WebUI，推荐 CLI 优先——harness 核心价值在终端，WebUI 做轻量仪表盘满足交付要求即可。

**我的决策：** 采纳 CLI 优先。

**后续发现：** 在设计的后期（补缺口时），我们发现 HITL 审批在自动化模式下需要一个不在终端也能操作的入口——这时 WebUI 的设计才真正有了明确的职责：暴露 HITL 审批端点。这说明"CLI 优先 + WebUI 为辅"的定位是对的，但 WebUI 的具体功能是在后期才被需求倒逼出来的。

---

### 节点 4：分发方式 — npm vs 二进制 vs Docker

**智能体的初始推荐：** 二进制为主（bun build）+ Docker 兜底。

**我引入的外部意见：** "TypeScript 项目做二进制是个坑——shell 执行、路径解析、跨平台调试都不增加 harness 工程深度。npm 为主 + Docker 兜底更务实。"

**智能体的回应：** 同意修正——目标用户是开发者，npm 是自然入口，`"bin"` 字段一行搞定。

**我的决策：** 推翻初始推荐，采用 npm + Docker。

**反思：** 这是整个 brainstorming 中唯一一次我引入外部意见推翻智能体推荐的决策。智能体的初始推荐有技术合理性（单文件分发确实方便），但忽略了"TypeScript 二进制打包的实际痛点"和"目标用户天然有 Node.js"这两个关键约束。这印证了 brainstorming 的价值：不是盲从 AI，而是把它作为一个对话对象来检视自己的假设。

---

### 节点 5：凭据存储 — 加密文件 + 主密码 vs 系统钥匙串

**智能体的推荐：** 加密文件 + 主密码（AES-256-GCM + PBKDF2），理由是跨平台一致、对 mock 测试友好。

**我的决策：** 采纳。

**反思：** 这个决策没有太多讨论就通过了。但在 SPEC 写完后我才意识到，这个方案的真正优势不是"跨平台"，而是"可测试"——你可以 mock 一个加密文件、用已知密码解密、断言返回的 key 正确。如果用了系统钥匙串，单元测试就要调 OS API。

---

## 三、至少 3 轮关键迭代

### 迭代 1：Agent 主循环的流程图修正

**智能体初始版本：** 步骤⑥和⑦的编号与流向不一致，HITL 分叉没有显式画出三条路径，停机判断被挤到角落。

**我的反馈：** 三点修正——停机判断应该是循环出口闸而非附庸、HITL 需要显式画出 approve/deny/timeout 三条分叉、步骤⑥和⑦的职责需要拆分（⑥ = 结果处理，⑦ = 纯停机判断）。

**智能体的修正：** 重新绘制流程图，采纳全部三点，补充了 deny → 反馈 → 停机判断 和 confirm → HITL 等待 → 三条结果路径。

**处理决策：** 我认同智能体的修正版本，确认通过。

**教训：** 如果这个流程图在 SPEC 里有歧义，agent 实现主循环时就会漏掉处理分支。图上一个分叉画漏了，agent 就会漏掉一个处理分支。这个迭代印证了"SPEC 里的图要精确到可以直接照着写代码"的重要性。

---

### 迭代 2：WebUI 和自动 Check 机制的缺口补全

**发现过程：** 九节设计全部确认完毕后，我在审查时发现两个缺口——WebUI 在架构图里画了但完全没设计；run-test 和 lint/typecheck 的职责边界不清，反馈提取器要解析输出但没有原子工具。

**智能体的补充设计：**
- WebUI：三个端点 + SSE 事件流 + 仅自动化模式启动
- Checks：配置驱动，自动运行，不占工具通道。同时从工具集中移除 `run_test`

**处理决策：** 采纳。这两个补充让设计从"逻辑自洽"变为"真正可交付"。

**教训：** 架构总览图是一面镜子。画图的过程会暴露出"你以为是共识但其实没讨论过"的空白。如果跳过架构总览直接写 SPEC，这两个缺口就会被带到实现阶段才发现。

---

### 迭代 3：护栏危险等级的动态化

**智能体的初始设计：** 每个工具挂一个静态的 `dangerLevel` 属性。

**我的反馈：** 同一个工具在不同参数下风险完全不同——`execute_shell("ls")` 和 `execute_shell("rm -rf /")` 不应该是同一个等级。`dangerLevel` 应该由护栏根据工具名 + 参数动态判定。

**智能体的修正：** 采纳，改为 `assessRisk(toolName, params)` 动态评估。工具定义只保留 `riskHint`，最终裁定权在护栏引擎。

**处理决策：** 采纳修正。

**教训：** 这是设计层面的一个关键修正。如果静态等级保留到实现阶段，护栏就会把 `ls` 和 `rm -rf /` 标为同一风险——等于没有护栏。这个问题是"提示词式护栏"和"代码式护栏"分界线的典型案例。

---

## 四、AI 建议的采纳与推翻

### 采纳的 AI 建议

| 建议 | 采纳理由 |
|------|----------|
| TypeScript 优先 | 静态类型对 TDD + subagent 迭代的价值超过初始学习成本 |
| 治理护栏为深度维度 | 天然确定性，最容易满足"移除 LLM 后可用 mock 测试"的要求 |
| 中间件管道架构（方案 A） | 线性管道 + 短路阻断，天然契合护栏的语义 |
| CLI 优先 | harness 的核心价值在终端 |
| 加密文件 + 主密码 | 跨平台一致 + 可 mock 测试 |
| 三层纵深防御（黑名单 → 规则 → 范围围栏） | 工业级纵深设计，职责清晰 |
| 反馈闭环全字符串解析 | 完全确定性，零 LLM 依赖 |
| 决策记忆两级指纹匹配 | 精确放行 + 模糊降级，兼顾安全和体验 |
| 配置驱动护栏规则 | 规则引擎是代码，具体规则是数据——完美满足 A.4 要求 |
| Checks 依赖容错 | 避免目标项目缺工具时每轮假阳性失败 |

### 推翻或修正的 AI 建议

| 建议 | 推翻/修正理由 |
|------|-------------|
| 二进制分发（bun build） | TypeScript 二进制打包对 shell 执行项目是坑，npm 更自然 |
| 工具静态 dangerLevel | 同一工具不同参数风险完全不同，必须动态评估 |
| 主循环流程图初版（步骤编号混乱） | ⑥⑦职责不清、HITL 分叉遗漏、停机判断位置错误 |
| key status 显示尾部 4 位（`****-xxxx`） | 对学生项目来说，连尾部也不该暴露，`(configured)` 足够 |

---

## 五、对 Brainstorming 技能的反思

### 做得好的地方

1. **"一次一个问题"的节奏控制。** 如果一次性抛出 5 个选择题，我可能会随便选几个然后后悔。每次一个问题让我有充分的时间思考和追问。语言选型那个节点就是最好的例子——如果和其他问题混在一起问，我不会追问"AI 生成代码的出错率"这个维度。

2. **逐节签字确认。** 九节设计每一节都停下来等我确认，这让设计过程有清晰的 checkpoint。护栏危险等级的动态化修正就是在这个机制下被发现的——如果一次性呈现全部设计然后签字，那个问题大概率会被漏掉。

3. **"先推荐再问"的模式。** 智能体不是机械地列出 A/B/C 然后等我选——它给出推荐并解释理由，这让我有锚定点可以反驳。如果它只是中性地列出选项，我的决策质量会低很多。

4. **被推翻后的态度。** 当我把外部意见（npm vs 二进制）抛给它时，它没有防御性地坚持原建议，而是承认对方的判断更准确并解释原因。这让 brainstorming 保持了"协作设计"的基调。

### 让我不满的地方

1. **WebUI 缺口是自己暴露的，不是智能体主动发现的。** 在九节设计确认完毕后，是我自己在审查时发现 WebUI 完全没有设计。智能体应该在架构总览环节就主动问"你提到了 WebUI，但还没有设计它的行为，要不要现在讨论？"——但它没有。这暴露了 brainstorming 的一个盲区：它善于在你给出的方向上深入，但不太擅长发现"你漏掉了什么"。

2. **某些推荐没有充分说明前提假设。** 比如"二进制分发"的推荐，它没有先说"这个推荐假设你的项目不做 shell 执行"——如果它把这个假设说出来，我当场就会反驳。推荐的合理性往往藏在前提假设里，而 AI 倾向于把假设隐藏。

3. **设计确认后的"收尾感"过强。** 九节确认完后有一种"设计完成了"的氛围，但实际上还有两个缺口。brainstorming 应该在最后主动触发一轮"完整性检查"——扫描需求文档的每一项，逐一核对设计是否覆盖。
