# 反思报告（REFLECTION.md）

本项目是一个 TypeScript 实现的 Coding Agent Harness——用确定性代码（而非提示词）完成主循环、工具分发、治理护栏、反馈闭环与凭据分发。开发全程用 Superpowers 方法论驱动：brainstorming 产出 SPEC，writing-plans 拆成 16 个 task，subagent 逐 task 实现，TDD 强制先红后绿，最终 45 个 commit、17 个 PR、495 个 mock-LLM 单元测试。以下按课程要求的九个问题逐一反思。

## 1. 哪些 Superpowers 技能发挥了最大作用？哪些"形式大于实质"？

真正起作用的是三个：**brainstorming**、**test-driven-development**、**requesting-code-review**。brainstorming 把"我想做一个 agent harness"这种模糊意图，通过一轮轮"一次只问一个问题"收敛成一份 11 章的 SPEC——尤其是治理护栏这个重点维度，如果不是被追问"哪个机制最体现工程深度"，我很可能六个维度都浅尝辄止。TDD 是另一根支柱：495 个 mock 测试是护栏、反馈、停机这些确定性机制能"移除 LLM 后仍可验证"的唯一保证。review 则直接催生了大量跨 task 修复——HITL 输入串扰、范围围栏硬拒绝失效、上下文预算缺失，全都是 review 阶段挑出来的。

形式大于实质的是 **using-git-worktrees** 和 **finishing-a-development-branch**。单人项目里，worktree 隔离带来的"并行开发"收益几乎为零，我用 PR 工作流就完全够用了；而"分支如何收尾"这种决策，在个人项目里答案很直接（merge 进 main），这个技能更像在替一个多人协作场景预留仪式感，而不是解决我当下真实遇到的问题。

## 2. TDD 强制在 AI 协作下是阻碍还是放大器？

对核心机制而言，TDD 是**放大器**。最有力的证据是范围围栏的一个 bug：`scope-fence` 的三个 Windows 路径用例（`..\` 穿越、`D:\` 盘符、`> C:\Windows\...`）在我本机 Windows 上全绿，却在 CI 的 Linux 上全红。如果当时是"先写实现、测试后补"，这个平台相关漏洞大概率永远不会被撞出来——因为我自己在 Windows 上根本看不到它。测试先行逼着我在写实现前就想清楚"越界路径应该有哪些形态"，这种前置思考本身就提升了实现质量。

阻碍的一面也存在，但只出现在**不该用 TDD 的地方**：纯脚手架（Task 0）和纯交付面（Task 15 的 Dockerfile/README/CI）根本没有可单测的代码，硬套"先红后绿"只是在浪费 subagent 的 token。结论是：TDD 是"确定性机制"的放大器，但不应被当成无差别的仪式。

## 3. subagent-driven 工作流让智能体能自主运行多久而不偏离主题？

**单一职责的 task 几乎能一口气做对**。Task 1（LLM 抽象层）、Task 3（凭据存储）这类"接口清晰、边界明确"的任务，subagent 产出后只需一次 review 微调。**跨模块边界的 task 则频繁偏离**。最典型的是 Task 12（Agent 主循环）——AGENT_LOG 记录过一个 subagent 把主循环写成了递归函数、没有停机判断、直接忽略 tool_calls，完全偏离 SPEC 的 7 步流程图，我不得不重写 prompt 并派发第二个 agent。

规律很清楚：一旦 task 涉及"多个模块的调用顺序"或"平台/协议语义"这类隐性上下文，subagent 就会在你没写明的每个假设处翻车。这些翻车点，恰恰是 SPEC 和 PLAN 质量的试金石。

## 4. 什么样的 task 颗粒度最优？

最优颗粒度不是行数，而是**"一个 subagent 能否在不读全局上下文的前提下做对"**。Task 3（凭据）颗粒度刚好：一个类、明确接口、19 个测试列清楚了边界条件（错误密码、损坏文件、Base64 非法……），subagent 一次成功。Task 0（脚手架）太细——拆成独立 task 的价值很低，几乎是仪式。Task 12（AgentLoop）太粗——一个 task 塞进了 6 个维度的编排，才导致递归函数那种彻底跑偏。

我的结论：颗粒度的下限是"有可单测的确定性逻辑"，上限是"不跨越模块边界"。两者之间才是 subagent 的舒适区。

## 5. SPEC / PLAN 质量如何影响实现质量（具体案例）

SPEC 质量足够——它定义了"做什么"和"为什么"，方向没有出过大错。问题几乎都出在 **PLAN 的代码示例**上。最直接的案例是 Task 15 的 Dockerfile：PLAN 给的示例是 `npm install -g ai4se-harness`，但这个包根本没发布到 npm，照抄必然失败，最终只能改成本地构建（`npm ci` + `npm run build` + `npm install -g .`）。另一个是 Task 16：PLAN 写"修改 `src/core/types.ts` 扩展 AgentResult"，但真实的 `AgentResult` 定义在 `src/core/agent-loop.ts`。

这两个案例说明同一件事：**PLAN 里作为"参考框架"的代码是不可信的**。正因为如此，我在每个 task 的派发 prompt 里都强制加了"PLAN 代码只是参考，以实际 `types.ts` 为准"这条约束——它有效避免了 subagent 照抄过期示例。

## 6. 你最有效的 prompt / context 策略是什么？

最有效的是**显式写出"不要做什么"**，而不是"要做什么"。三条反复生效的约束：①"零网络依赖，用 `MockLLMProvider`，不连真实 API"——保证了测试在任何机器上可重复；②"不要修改 task 范围外的文件"——防止 subagent 顺手重构无关模块；③"PLAN 示例不能照抄，先读实际代码"——这条直接化解了第 5 点里的 PLAN 过期问题。

我学到的更深一层是：subagent 最容易出错的不是"做不对"，而是"多做了"和"照着错的做"。因此，约束式的 prompt（划边界、列禁区）比目标式的 prompt（描述要什么）更能拉高产出质量。

## 7. 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

如果没有这两条要求，我大概率会写死一个 key 或者用 `export` 凑合。它们逼我补上了两类认知：**凭据的三态权衡**——硬编码（最差）、环境变量（明文、进 shell history）、加密文件 + 主密码（安全但每次要解密）。最终选了 PBKDF2(100k) + AES-256-GCM 加密存 `~/.ai4se-harness/credentials.enc`，并把"明文 key 只在进程内存"这条边界写进 SPEC 威胁模型。

**分发**则把我从"能跑"逼到"别人能在一台全新机器上安全地跑"。这个过程中踩的全是真实坑：Docker 镜像源失效、npm 全局安装的 symlink 导致 CLI 静默退出、Windows 路径在 Linux 容器里失效。这些坑只有在真正做分发时才会暴露——它们是"从零运行"这条验收标准的价值所在。

## 8. 如果重做你会改变什么？

第一，**范围围栏一开始就做平台无关化**，而不是等到 CI 在 Linux 上挂了三个用例才补。第二，**CI 从第一个 task 起就包含 docker build**，而不是交付前才意识到漏了。第三，**PLAN 的 checkbox 持续更新**，而不是最后统一补——过程记录一旦滞后，AGENT_LOG 和 PLAN 就逐渐失真。第四，**CLI 入口检测一开始就用 realpath**，而不是依赖 `argv[1]` 字符串匹配，埋下一个只会在 symlink 下暴露的雷。

这些改变的共同点：都属于"早期花小成本、后期能省大返工"的前置投入。

## 9. 你对 Superpowers 方法论的批判——它假设了什么，这些假设成立吗？

Superpowers 至少假设了三件事，其中两件在本项目里不成立。**假设一：陌生 agent 冷启动能暴露 spec 缺陷**。我确实做了冷启动验证（换独立 agent 试跑 Task 1/4），但它暴露的问题并没有被系统性地回填进 SPEC——这条"最关键客观证据"最终没有兑现它应有的价值，说明方法论假设了"验证 → 回填"这条闭环会自动发生，而实际上它需要额外的人为纪律。**假设二：worktree 隔离更高效**。单人项目里这完全不成立，PR 工作流已足够。

**假设三：流程纪律能保证质量**——这个假设部分成立。Superpowers 守住了 TDD、review、计划这些"容易在 AI 协作中松懈"的纪律，这是它的最大价值；但它不能替你回答"做什么"和"做对了吗"。我体会到的是：Superpowers 是一套很好的流程脚手架，但它解决的是"怎么不跑偏"，而不是"该往哪走"。后者，恰恰是当 LLM 能完成大部分编码时，工程师真正剩下的价值。
